from django.apps import AppConfig


class InsuranceExplainerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.insurance_explainer"
    verbose_name = "Insurance Explainer"
